# This file is placed in the Public Domain.
# doctest: +ELLIPSIS


"brokers"


>>> from nixbot.defines import Broker, Object
>>> b = Broker()
>>> o = Object()
>>> repr(o) # doctest: +ELLIPSIS
'<nixbot.objects.Object object at ...'
>>> id = b.add(o)
>>> print(id) # doctest: +ELLIPSIS
<nixbot.objects.Object object at ...>
>>> obj = b.get(id)
>>> repr(obj) == id
True
